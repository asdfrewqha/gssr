# HTML — always fetch fresh so users get the latest JS/CSS hashes
/index.html
  Cache-Control: no-cache, no-store, must-revalidate

# Hashed assets — immutable forever (filename changes on each build)
/assets/*
  Cache-Control: public, max-age=31536000, immutable
