# services/workers — Python FastAPI + Celery workers
# Copy to .env and fill in values for local development.
# pydantic-settings reads this file automatically (env_file = ".env").

# ── Database ─────────────────────────────
# asyncpg driver required for SQLAlchemy async
WORKERS_DATABASE_URL=postgresql+asyncpg://gssr:changeme@localhost:5432/gssr

# ── Valkey / Redis (Celery result backend) ──
VALKEY_URL=redis://localhost:6379

# ── RabbitMQ (Celery broker) ──────────────
RABBITMQ_URL=amqp://gssr:changeme@localhost:5672/

# ── MinIO S3 ─────────────────────────────
MINIO_ENDPOINT=localhost:9000
MINIO_PUBLIC_URL=http://localhost:9000
MINIO_ACCESS_KEY=gssr
MINIO_SECRET_KEY=changeme
MINIO_BUCKET=gssr
MINIO_SECURE=false

# ── NSFW moderation model ────────────────
# Download the model first: see docs/steps/04-worker-service.md
NSFW_MODEL_PATH=models/nsfw.onnx
NSFW_THRESHOLD=0.7

# ── Admin auth ───────────────────────────
# Must match JWT_SECRET in services/game/.env so admin panel JWTs are accepted
ADMIN_JWT_SECRET=replace-with-64-char-random-string-generate-with-openssl

# ── CORS ─────────────────────────────────
# Admin panel Vite dev server port
CORS_ORIGINS=http://localhost:5174
