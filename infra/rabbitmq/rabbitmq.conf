## RabbitMQ configuration
## management.path_prefix tells the management UI it is served under /utils/mq/
## so all asset URLs, redirects, and API calls use the correct public path.
management.path_prefix = /utils/mq
