name: gssr-test

# Isolated test environment — ephemeral DB/cache, no Nginx, no Cloudflare.
# Usage: docker compose -f infra/compose/docker-compose.test.yml up -d
#        <run tests>
#        docker compose -f infra/compose/docker-compose.test.yml down -v

services:
  postgres-test:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: gssr_test
      POSTGRES_PASSWORD: gssr_test
      POSTGRES_DB: gssr_test
    ports:
      - "5433:5432"    # offset to avoid conflict with dev postgres on 5432
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U gssr_test -d gssr_test"]
      interval: 5s
      timeout: 3s
      retries: 10
    tmpfs:
      - /var/lib/postgresql/data   # in-memory — fast, no leftover state

  valkey-test:
    image: valkey/valkey:7-alpine
    ports:
      - "6380:6379"    # offset to avoid conflict with dev valkey

  rabbitmq-test:
    image: rabbitmq:3.13-alpine
    ports:
      - "5673:5672"    # offset

  minio-test:
    image: minio/minio:latest
    command: server /data
    environment:
      MINIO_ROOT_USER: gssr_test
      MINIO_ROOT_PASSWORD: gssr_test
    ports:
      - "9002:9000"
    tmpfs:
      - /data

  # Run goose migrations before tests (built from golang:alpine — no ghcr.io auth needed)
  migrate:
    image: golang:1.22-alpine
    command: >
      sh -c "[ -f /tmp/go/bin/goose ] ||
      go install github.com/pressly/goose/v3/cmd/goose@v3.20.0 &&
      /tmp/go/bin/goose -dir /migrations postgres
      'postgres://gssr_test:gssr_test@postgres-test:5432/gssr_test?sslmode=disable' up"
    environment:
      GOPATH: /tmp/go
      GOMODCACHE: /tmp/gomod
    volumes:
      - ../../migrations:/migrations:ro
      - goose-bin:/tmp/go      # caches the compiled goose binary
      - goose-mod:/tmp/gomod   # caches downloaded module sources
    depends_on:
      postgres-test:
        condition: service_healthy
    restart: on-failure

  # Game service for E2E tests
  game-test:
    build:
      context: ../../services/game
      dockerfile: Dockerfile
      args:
        TARGETARCH: amd64
    environment:
      PORT: "3000"
      METRICS_PORT: "2112"
      POSTGRES_URL: postgres://gssr_test:gssr_test@postgres-test:5432/gssr_test?sslmode=disable
      VALKEY_URL: redis://valkey-test:6379
      JWT_SECRET: test-secret-do-not-use-in-production-min-32chars
      JWT_ACCESS_TTL: 15m
      JWT_REFRESH_TTL: 168h
      LIVEKIT_URL: ""
      LIVEKIT_API_KEY: ""
      LIVEKIT_API_SECRET: ""
      CORS_ORIGINS: "http://localhost:5173"
    ports:
      - "3001:3000"    # offset for E2E
    depends_on:
      migrate:
        condition: service_completed_successfully
      valkey-test:
        condition: service_started
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:3000/health || exit 1"]
      interval: 5s
      timeout: 3s
      retries: 10

  # Workers service for E2E tests
  workers-test:
    build:
      context: ../../services/workers
      dockerfile: Dockerfile
    environment:
      DATABASE_URL: postgresql+asyncpg://gssr_test:gssr_test@postgres-test:5432/gssr_test
      VALKEY_URL: redis://valkey-test:6379
      RABBITMQ_URL: amqp://guest:guest@rabbitmq-test:5672/
      MINIO_ENDPOINT: minio-test:9000
      MINIO_ACCESS_KEY: gssr_test
      MINIO_SECRET_KEY: gssr_test
      MINIO_BUCKET: gssr-test
      MINIO_SECURE: "false"
      NSFW_MODEL_PATH: /app/models/nsfw.onnx
      NSFW_THRESHOLD: "0.7"
      ADMIN_JWT_SECRET: test-secret-do-not-use-in-production-min-32chars
      CORS_ORIGINS: "http://localhost:5174"
    ports:
      - "8001:8000"    # offset for E2E
    depends_on:
      migrate:
        condition: service_completed_successfully
      rabbitmq-test:
        condition: service_started
      minio-test:
        condition: service_started
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://localhost:8000/health')\""]
      interval: 5s
      timeout: 3s
      retries: 15

volumes:
  goose-bin:    # compiled goose binary — survives make test-down if you omit -v
  goose-mod:    # go module download cache — avoids re-fetching on every test-up
